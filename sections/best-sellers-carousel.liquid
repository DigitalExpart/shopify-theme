{%- comment -%}
  Best Sellers Carousel Section
{%- endcomment -%}

{%- liquid
  assign best_coll = section.settings.collection
  assign products_to_show = section.settings.products_to_show | default: 6
-%}

<div class="best-sellers-carousel section-spacing" style="background-color: var(--color-bg-primary);">
  <div class="page-width">
    <div class="flex-between" style="margin-bottom: 2.5rem;">
      <div>
        <span class="subheading">{{ section.settings.subheading | default: 'Customer Favorites' }}</span>
        <h2>{{ section.settings.heading | default: 'Best Sellers' }}</h2>
      </div>

      <div class="flex items-center gap-sm">
        <button type="button" class="icon-btn" data-carousel-prev aria-label="Previous" style="background: var(--color-bg-secondary); border: 1px solid var(--color-border);">
          &larr;
        </button>
        <button type="button" class="icon-btn" data-carousel-next aria-label="Next" style="background: var(--color-bg-secondary); border: 1px solid var(--color-border);">
          &rarr;
        </button>
      </div>
    </div>

    <div data-carousel style="position: relative;">
      <div data-carousel-track style="display: flex; gap: 1.5rem; overflow-x: auto; scroll-snap-type: x mandatory; padding-bottom: 1rem; scrollbar-width: none;">
        {%- if best_coll != blank and best_coll.products.size > 0 -%}
          {%- for product in best_coll.products limit: products_to_show -%}
            <div style="flex: 0 0 280px; scroll-snap-align: start;">
              {% render 'product-card', product_card_product: product, show_vendor: true %}
            </div>
          {%- endfor -%}
        {%- else -%}
          {%- for i in (1..6) -%}
            <div style="flex: 0 0 280px; scroll-snap-align: start;">
              {% render 'product-card', show_vendor: true %}
            </div>
          {%- endfor -%}
        {%- endif -%}
      </div>
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Best Sellers Carousel",
  "settings": [
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading",
      "default": "Customer Favorites"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Best Sellers"
    },
    {
      "type": "collection",
      "id": "collection",
      "label": "Collection"
    },
    {
      "type": "range",
      "id": "products_to_show",
      "min": 4,
      "max": 12,
      "step": 2,
      "default": 8,
      "label": "Products to Show"
    }
  ],
  "presets": [
    {
      "name": "Best Sellers Carousel"
    }
  ]
}
{% endschema %}
